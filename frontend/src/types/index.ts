export type Credential = {
  email: string
  password: string
}


export type Record = {
    id: string
    memo: string
    duration: number
}

export type ChartData = {
  id: string
  duration: number
  day: string
}